# Blocks

This folder will hold higher-level UI sections and reusable patterns.

Planned contents:

- hero sections
- feature grids
- auth screens
- dashboard sections
- marketing patterns

Current intent:

- keep blocks thin and composable
- build them from shared tokens and components
- let the Astro starter consume them without framework-specific coupling
